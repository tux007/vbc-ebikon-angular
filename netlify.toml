[build]
  command = "npm run build"
  publish = "dist/vbc-ebikon-angular/browser"

[build.environment]
  NODE_VERSION = "20"

# SPA Routing: alle Pfade auf index.html umleiten
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
